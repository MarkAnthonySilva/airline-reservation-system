Correlated Subquery

SELECT * FROM passenger p, airline a
WHERE a.aID = ? AND (aID, pID) IN 
 (SELECT aID, pID FROM blacklist b WHERE b.pID  = p.pID AND b.aID = a.aID);

GROUP BY and HAVING

Aggregation

Outer join

Mathmatical set operation